&defaults_reporting

failure_screenshots: true
resize_for_screenshots: false

local:

  html_report: true
  realtime_failure: false
  junit: true
  json: true
  rerun: true

dashboard:
  enabled: false
  dashboard_url: https://ORG_ID.beta.testevolve.net
  label: kyuri
  auth_token: api_key
  scheduled_updates: false
  scheduled_interval: 10


<%= IO.read(filename) if File.exists?(filename) %>
